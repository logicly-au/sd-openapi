use SD::OpenAPI::Test;

# Just by way of orthogonality, the code is writen to handle these, but I can't
# think of a way that a null parameter can be specified.
pass('I can\'t think of a way to pass a null non-body parameter.');

done_testing;
